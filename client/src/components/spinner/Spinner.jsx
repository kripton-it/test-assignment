import React from 'react';

import * as Styles from './styles';

const Spinner = () => {
  return (
    <Styles.SpinnerContainer>
      <Styles.SpinnerBody />
    </Styles.SpinnerContainer>
  );
};

export default Spinner;